{
  "name": "aquadrain-online",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp index.html sitemap.xml robots.txt public/"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
