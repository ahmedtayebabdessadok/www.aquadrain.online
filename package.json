{
  "name": "aquadrain-7-files-deploy",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp index.html sitemap.xml robots.txt llms.txt public/ && tar -xzf aquadrain-pages.tar.gz -C public"
  }
}